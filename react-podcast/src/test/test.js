
test('Array', function() {
  test('#indexOf()', function() {
    it('should return 3 when 1 + 2 ', function() {
        expect(1 + 2, " One plus two equals three").equals(5, "should be three");
    });
  });
});
